/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "assets.aceternity.com"], // Add any other domains you need to load images from
  },
};

export default nextConfig;
